Hello Applicant! <br/>
Please answer the following <%= @questions.count %> questions:<br/>

<%= semantic_form_for @applicant, :url => general_answer_path(@applicant) do |form|%>
    <%= form.semantic_fields_for :general_answers do |answer| %>
        <%= answer.inputs :id, :hidden => 'true'%>
        <% @index = form_object_index(answer.object_name).to_s.to_i %>
                <%= @questions[@index.to_i].question %><br/>
    <%= answer.inputs :answer, :label => 'ham' %>
    <%end%>
    <%= form.buttons %>
    <% end %>
<br/><br />
or just skip to the <%= link_to 'next.', job_applications_next_path(:applicant => @applicant.id) %> section.